# 项目永久约束与经验规则 (.rules)

## 1. 端口与本地环境约束
- **端口 conflict 说明**：macOS 系统级服务 `ControlCenter` (AirPlay 接收器) 默认占用了 `7000` 端口 (`afs3-fileserver`)。
- **固定端口决策**：本地开发环境与容器端口统一固定映射使用 **`7001`** 端口，严禁试图在 `7000` 端口启动服务。

## 2. 远程服务器 (VPS) 部署别名与 IP 约束
- **VPS 2 (关键)**：用户的 `vps2` 设备专指 **`154.12.85.111`** (端口 22, root 用户)。在本地 Shell 中 alias 为 `vps2`，在 `~/.ssh/config` 中 Host 别名为 `new-vps`。
- **部署踩坑教训**：部署生产环境时，务必准确识别 `vps2` 对应的 IP (`154.12.85.111`) 或别名 `new-vps`，绝不可误部署至 `myvps` (`207.57.146.226`) 或 `vps` (`117.55.236.41`) 等其他机器节点。

## 3. CI/CD GitHub Actions 与 Watchtower 自动部署约束
- **GHCR 私有镜像拉取说明**：私有包在远端直接 `docker pull` 会报 `manifest unknown`。如需使用 Watchtower 自动化零感拉取，推荐将 GHCR Package 可见性设为 Public，或者在 Compose 中保留 `build: .` 兜底本地构建。

## 4. UI/UX 图标与视觉审美约束
- **严禁使用 Emoji/文字伪图标**：在按钮、操作菜单、状态提示面板与导航组件中，严禁直接使用文字/Emoji 字符 (如 🔍、🚀、🎉、✅、❌ 等) 作为图标。所有图标必须统一使用高质量、规范的 **SVG 矢量图形 (Lucide / Feather 风格)**，保持全站界面视觉体验的高高级与极客质感。
- **严禁使用原生 confirm()/alert() 对话框**：全站禁止使用浏览器原生的 `window.confirm()` 或 `window.alert()` 阻塞对话框（其视觉体验极其原始丑陋）。所有交互确认必须统一调用极具现代感的 Glassmorphism 异步 Promise 自定义确认弹窗组件 (`showConfirmDialog`)。
